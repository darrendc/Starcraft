<h1>
  Faction Information
</h1>

<h2>
  <u>
    <%= @faction.name %>
  </u>
</h2>
<h3>
  <%= @faction.description %>
</h3>

<p>
  <%= link_to "Logout", session_path, method: :delete %>
</p>
<%= link_to "Create new Planet for #{@faction.name} colonization", new_faction_planet_path(@faction) %>
<p>
  <%= link_to "<-Back to #{@player.name}'s Dashboard", player_path(@player) %>
</p>
